import { Router } from 'express'

const router = Router()

// Legacy content-unit n8n endpoints (sync-yadisk pending/mark-published)
// were removed in the content-bank rework. This router is a placeholder
// for any future n8n integrations.

export default router
